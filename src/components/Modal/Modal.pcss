.modal {
  position: absolute;
  left: 0;
  top: 0;
  right: 0;
  bottom: 0;
}

.modal__mask {
  width: 100%;
  height: 100%;
  background-color: rgba(0,0,0, .5);
  display: flex;
}

.modal__wrapper {
  margin: auto;
}
